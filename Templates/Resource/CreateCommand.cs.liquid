using {{ ProjectName }}.Application.Abstractions.Messaging;

namespace {{ ProjectName }}.Application.{{ ResourceName }}s.Create;

public sealed record Create{{ ResourceName }}Command() : ICommand<Guid>;
