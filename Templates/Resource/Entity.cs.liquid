using {{ ProjectName }}.Domain.Primitives;

namespace {{ ProjectName }}.Domain.Entities;

public sealed class {{ ResourceName }} : Entity
{
    public {{ ResourceName }}(Guid id) : base(id)
    {
    }

    private {{ ResourceName }}()
    {
    }
}
